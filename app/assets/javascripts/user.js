(function($, window, document) {
	"use strict";

	/*
	*	@module
	*/
	App.User = (function () {

		var unresolvedBlock;

		function init() {
			unresolvedBlock = $('#unresolved.block');
		}

		function buildUnresolvedBlock () {
			
		}

		return {
			init : init
		};
	});


}(jQuery, this, this.document));